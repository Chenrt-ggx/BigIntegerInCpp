#ifndef BIGINTEGER_CONSTANT_H
#define BIGINTEGER_CONSTANT_H

#include <string>
#include <vector>

const int width = 4;

const int mask = 10000;

std::string piCalculate(int length);

std::string eCalculate(int length);

#endif //BIGINTEGER_CONSTANT_H
